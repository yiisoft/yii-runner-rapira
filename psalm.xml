<?xml version="1.0"?>
<psalm
    errorLevel="1"
    findUnusedBaselineEntry="true"
    findUnusedCode="false"
    ensureOverrideAttribute="false"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
>
    <stubs>
        <!-- The contract ships its function declarations outside the autoloader. -->
        <file name="vendor/rapira/contract/src/functions.php" />
        <!-- Worker-mode functions are provided by the runtime, not the contract. -->
        <file name="stubs/rapira.php" />
    </stubs>
    <projectFiles>
        <directory name="src" />
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>
    <issueHandlers>
        <MixedAssignment errorLevel="suppress" />
        <RiskyTruthyFalsyComparison errorLevel="suppress" />
    </issueHandlers>
</psalm>
